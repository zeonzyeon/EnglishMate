# Application
spring.application.name=EnglishMate

# Server
server.port=8080

# Thymeleaf
spring.thymeleaf.cache=false

# Database
# TODO: Configure MySQL connection before implementing persistence features.
# spring.datasource.url=jdbc:mysql://localhost:3306/englishmate?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
# spring.datasource.username=englishmate
# spring.datasource.password=your-password
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA
# Keep database auto-configuration disabled until the MySQL connection is ready.
# Remove this line after enabling spring.datasource.* settings above.
spring.autoconfigure.exclude=org.springframework.boot.jdbc.autoconfigure.DataSourceAutoConfiguration,org.springframework.boot.hibernate.autoconfigure.HibernateJpaAutoConfiguration
# TODO: Enable JPA settings after configuring MySQL.
# spring.jpa.hibernate.ddl-auto=none
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.format_sql=true
